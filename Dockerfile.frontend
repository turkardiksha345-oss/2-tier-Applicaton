# Use the official nginx image
FROM nginx:alpine

# Copy the frontend files to the nginx html directory
COPY frontend/ /usr/share/nginx/html/

# Expose port 80
EXPOSE 80

# The default nginx configuration will serve the files