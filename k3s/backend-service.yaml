apiVersion: v1
kind: Service
metadata:
  name: rock-paper-scissors-backend-service
spec:
  selector:
    app: rock-paper-scissors-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP