apiVersion: v1
kind: Service
metadata:
  name: rock-paper-scissors-frontend-service
spec:
  selector:
    app: rock-paper-scissors-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort